package board;

import java.util.ArrayList;

public class Chain {

    public ChainType type;
    public ArrayList<Integer> boxes; // Stores the boxes in this chain in order (two possible orders)
    public int size; // Stores the size of boxes

}
